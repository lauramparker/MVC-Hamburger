<h1>Eat-Da-Burger!</h1> 


<div id = "container-burger" class="all-burgers col-9 mx-auto">


<div id = "not-yet-devoured">
    <h3>Not Yet Devoured!</h3> 
    <ul>
    {{#each burgers}}
      {{#unless devour}}
        {{!-- {{> change-condition devour=false}} --}}
        <li>
        <p>{{this.id}}    Name: {{this.name}}</p>
        <button class="change-status">Devour It!</button>
        </li>
      {{/unless}}
    {{/each}}
    </ul>
</div>


<div id = "devoured-it"></div>
    <h3>Devoured It!</h3>
    <ul> 
    {{#each burgers}}
        {{#if devour}}
        {{!-- {{> change-condition devour=true}} --}}
        <li>
        <p>{{this.id}}    Name: {{this.name}}</p>
        </li>
        {{/if}}
    {{/each}}
    </ul>
</div>


<div id = "add-a-burger">
    <h3>Add a Burger</h3>
    <form class="createNEW-form">

    <div class="form-group">
        <label for="hambrg">Burger Name:</label>
        <input type="text" id="hambrg" name="name">
    </div>

    <button type="submit">Add Burger</button>
    </form>
</div>


</div>